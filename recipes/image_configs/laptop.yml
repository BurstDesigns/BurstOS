# Configs for mobile devices

modules:

  - type: files
    files:
      - source: laptop # systemd preset files
        destination: /

  - type: dnf
    install:
      packages:
        - power-profiles-daemon
        - cups-pk-helper
