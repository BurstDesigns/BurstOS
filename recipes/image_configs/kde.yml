# KDE configuration for BurstOS images

modules:

  - type: files
    files:
      - source: kde
        destination: /

  - type: default-flatpaks
    configurations:
      - scope: user
        install:
          - org.kde.gwenview
          - org.kde.okular
