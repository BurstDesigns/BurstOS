# KDE configuration for BurstOS images

modules:

  - type: files
    files:
      - source: kde
        destination: /

  - type: default-flatpaks
    user:
      # repo-name: flathub-user
      install:
        - org.kde.gwenview
        - org.kde.okular
